#include "main.h"
/**
* _isupper - checks whether the character is uppercase
* @c: the character to be checked
* Return: always 1
*/
int _isupper(int c)
{
	for (c = 'A'; c <= 'Z'; c++)
	{
		return (1);
	}
	return (0);
}
